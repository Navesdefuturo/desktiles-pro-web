// Paddle desactivado — se activa cuando tengas tu cuenta Paddle
// Para activar: descomenta el código y reemplaza PADDLE_VENDOR_ID con tu ID real

export default function PaddleProvider() {
  return null
}

export function openPaddleCheckout(_productId: number, _email?: string) {
  // Paddle pendiente de configurar
  console.log('Paddle no configurado todavía')
}
